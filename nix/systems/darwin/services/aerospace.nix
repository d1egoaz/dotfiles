_:

{
  # AeroSpace window manager - now managed via Home Manager with external config
  # Config file: ~/.config/aerospace/aerospace.toml -> ~/dotfiles/config/aerospace/aerospace.toml
  # Service: Home Manager launchd agent (programs.aerospace.launchd.enable = true)
  services.aerospace.enable = false;
}
