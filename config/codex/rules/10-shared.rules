# Shared Codex approval rules synced via dotfiles.
#
# Keep portable, low-risk rules here so they can be pushed and reused across
# machines. Keep work-only rules in ~/.codex/rules/90-work-local.rules.
# Keep one-off local approvals in ~/.codex/rules/default.rules.

prefix_rule(pattern=["git", "add"], decision="allow")
prefix_rule(pattern=["git", "commit"], decision="allow")
prefix_rule(pattern=["git", "clone"], decision="allow")
prefix_rule(pattern=["git", "fetch"], decision="allow")
prefix_rule(pattern=["git", "push"], decision="allow")
prefix_rule(pattern=["git", "rebase"], decision="allow")
prefix_rule(pattern=["git", "cherry-pick"], decision="allow")

# Destructive commands ask, even where a broader rule above allows the command:
# the strictest matching decision wins. Rules match whole argv words from the
# start, with no wildcards, so this catches only the common spellings. Still
# allowed by `git push` above, and covered only by the destructive-change gate
# in config/ai/AGENTS.md: `--force-with-lease=<ref>`, a `+<ref>` refspec,
# combined short flags such as `-fu`, and a force flag after the remote or
# branch (`git push origin main --force`).
prefix_rule(pattern=["git", "push", "--force"], decision="prompt")
prefix_rule(pattern=["git", "push", "-f"], decision="prompt")
prefix_rule(pattern=["git", "push", "--force-with-lease"], decision="prompt")
prefix_rule(pattern=["git", "reset", "--hard"], decision="prompt")
prefix_rule(pattern=["git", "clean"], decision="prompt")
prefix_rule(pattern=["git", "branch", "-D"], decision="prompt")
prefix_rule(pattern=["git", "worktree", "remove", "--force"], decision="prompt")
prefix_rule(pattern=["rm", "-rf"], decision="prompt")
prefix_rule(pattern=["rm", "-fr"], decision="prompt")

prefix_rule(pattern=["gh", "auth", "status"], decision="allow")
prefix_rule(pattern=["gh", "issue", "view"], decision="allow")
prefix_rule(pattern=["gh", "pr", "create"], decision="allow")
prefix_rule(pattern=["gh", "pr", "view"], decision="allow")
prefix_rule(pattern=["gh", "pr", "checks"], decision="allow")
prefix_rule(pattern=["gh", "pr", "diff"], decision="allow")
prefix_rule(pattern=["gh", "search", "code"], decision="allow")
prefix_rule(pattern=["gh", "search", "issues"], decision="allow")
prefix_rule(pattern=["gh", "search", "prs"], decision="allow")
prefix_rule(pattern=["gh", "workflow", "list"], decision="allow")

prefix_rule(pattern=["tfctl", "--help"], decision="allow")
prefix_rule(pattern=["tfctl", "--version"], decision="allow")
prefix_rule(pattern=["tfctl", "auth", "status"], decision="allow")
prefix_rule(pattern=["tfctl", "profile", "display"], decision="allow")
prefix_rule(pattern=["tfctl", "profile", "get"], decision="allow")
prefix_rule(pattern=["tfctl", "profile", "profiles", "list"], decision="allow")
prefix_rule(pattern=["tfctl", "api", "schema", "search"], decision="allow")
prefix_rule(pattern=["tfctl", "api", "schema", "get"], decision="allow")
prefix_rule(pattern=["tfctl", "get"], decision="allow")
prefix_rule(pattern=["tfctl", "run", "status"], decision="allow")

# NOTE: prefix rules match argv literally, so ["git", "-C", "add"] only matches
# the nonsensical `git -C add ...` (the path argument occupies that position).
# `git -C <path> add` cannot be expressed as a literal prefix; rely on the plain
# ["git", "add"] style rules above and per-session approvals for -C variants.

prefix_rule(pattern=["jq"], decision="allow")
prefix_rule(pattern=["rg"], decision="allow")
prefix_rule(pattern=["sort"], decision="allow")
prefix_rule(pattern=["sleep"], decision="allow")

# Local Variables:
# mode: conf-toml
# End:
