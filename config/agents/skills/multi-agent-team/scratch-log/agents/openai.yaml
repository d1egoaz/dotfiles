interface:
  display_name: "Scratch Log"
  short_description: "Keep an explicitly requested decision log"
  default_prompt: "Use $scratch-log when the user explicitly requests a local decision log or audit notes."

policy:
  allow_implicit_invocation: false
