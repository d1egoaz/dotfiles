interface:
  display_name: "Multi-Agent Routing"
  short_description: "Route GPT-5.6 models by outcome value"
  default_prompt: "Use $multi-agent-routing to choose the best-value GPT-5.6 model and reasoning effort for the desired outcome, then delegate only when an independent task justifies it."

policy:
  allow_implicit_invocation: true
