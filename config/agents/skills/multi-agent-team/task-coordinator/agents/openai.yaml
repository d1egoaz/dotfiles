interface:
  display_name: "Task Coordinator"
  short_description: "Coordinate independent workstreams and monitoring"
  default_prompt: "Use $task-coordinator for multiple independent outcomes, repositories, visible tasks, or ongoing monitoring."

policy:
  allow_implicit_invocation: true
