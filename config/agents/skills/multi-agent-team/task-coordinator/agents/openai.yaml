interface:
  display_name: "Task Coordinator"
  short_description: "Route independent outcomes and verify completion"
  default_prompt: "Use $task-coordinator to split independent outcomes, select the cheapest capable routes, and coordinate child-owned work through approval, verification, and completion. Keep simple or tightly coupled work in the lead."

policy:
  allow_implicit_invocation: true
