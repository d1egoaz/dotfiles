interface:
  display_name: "tfctl"
  short_description: "Use HCP Terraform and TFE through tfctl"
  default_prompt: "Use $tfctl to inspect or manage HCP Terraform, Terraform Cloud, or Terraform Enterprise with the tfctl CLI."

policy:
  allow_implicit_invocation: true
